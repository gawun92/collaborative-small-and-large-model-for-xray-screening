# ── Core pipeline ─────────────────────────────────────────────────────────────
pyyaml
tqdm

# ── Large model: YOLOv11L ─────────────────────────────────────────────────────
# Install torch + torchvision FIRST via the official PyTorch installer
# (pytorch.org/get-started) to get the correct CUDA build, then install this.
ultralytics

# ── Small model: NanoDet-Plus ─────────────────────────────────────────────────
# Install the bundled nanodet package from source before running the pipeline:
#   pip install -e ./small_model/nanodet/
#
# Runtime inference dependencies for nanodet (not pulled in by ultralytics):
addict
omegaconf>=2.0.1
pytorch-lightning
termcolor
tabulate
pycocotools
